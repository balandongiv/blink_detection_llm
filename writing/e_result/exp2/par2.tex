The pipeline was evaluated under epoch durations of 10, 20, 30 (reference), 40, 50, 60 and 120 seconds.
For each duration the full pipeline is re-run from scratch, since epoch-level
peak-to-peak statistics change with epoch length.
Experiment~1 found that Fp1 and Fp2 were consistently the two best-performing electrodes on both corpora.
%Rather than re-applying a per-session,
%per-duration oracle over all 32 channels, this experiment fixes the operating point to
%that Fp1/Fp2 pair: for every session, Fp1's and Fp2's $F_1$, each scored inside the full
%32-channel montage run, are reported separately rather than averaged together, so each
%electrode's own epoch-duration behaviour remains visible.
Each electrode is thus a deployable single-electrode operating point and the epoch duration is being varied.
Figure~\ref{fig:f1_by_epoch} reports macro-averaged $F_1$ for each duration, with an
asterisk marking the durations whose two-tailed Wilcoxon $p$-value, Bonferroni-corrected
over the six non-reference durations within each electrode, differs significantly from
the 30-second reference.
