A detector tuned and reported on a single corpus may not transfer to data collected under
different acquisition conditions. Because the benchmark spans a closed corpus (Raja) and an open corpus (Cao2018), the generalisation gap can be quantified directly as the
per-condition difference in macro-$F_1$ between the two datasets.