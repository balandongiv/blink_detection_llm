\begin{figure}[htbp]
    \centering
    \fbox{\begin{minipage}{0.90\columnwidth}\centering\small
        Raw EEG recording (continuous, multichannel)\\[-1pt]
        $\downarrow$\\[-1pt]
        Downsample to \texttt{RESAMPLE\_RATE}~$=100$~Hz; band-pass filter from
        \texttt{FILTER\_LOW}~$=1.0$~Hz to \texttt{FILTER\_HIGH}~$=20.0$~Hz\\[-1pt]
        $\downarrow$\\[-1pt]
        Segment into non-overlapping \texttt{EPOCH\_DURATION\_S}~$=30$~s epochs and
        drop low-health epochs\\[-1pt]
        $\downarrow$\\[-1pt]
        \textbf{Stage A:} per-channel peak-to-peak autoreject screening flags
        suspicious epochs $\mathcal{B}$\\[-1pt]
        $\downarrow$\\[-1pt]
        \textbf{Stage B:} estimate $\theta=\xi+k(1.4826\cdot\mathrm{MAD})$ from
        flagged epochs (median or mean centre)\\[-1pt]
        $\downarrow$\\[-1pt]
        \textbf{Stage C:} scan threshold crossings and map blink regions to
        epoch-relative timing\\[-1pt]
        $\downarrow$\\[-1pt]
        Event-level evaluation: precision, recall, macro-averaged $F_1$, and
        Wilcoxon signed-rank tests
    \end{minipage}}
    \caption{Per-epoch blink-detection pipeline. The continuous recording is downsampled and band-pass filtered, segmented into fixed-length epochs with low-health epochs removed, and then passed through the three detection stages: Stage~A screens suspicious (blink-heavy) epochs by peak-to-peak amplitude, Stage~B estimates a robust sample-level amplitude  threshold from those epochs, and Stage~C localises blink regions by scanning threshold
    crossings. Detections are finally scored against the ground truth at the event level.}
    \label{fig:pipeline_flow}
\end{figure}